#include <stdio.h>
#define N 100
void revers(char a[], int n);
char a[N+1], b[N+1], c[N+2];
int main()
{
	fgets(a,N,stdin);
	fgets(b,N,stdin);
	add(a,b,res);
	printf("%s\n%s\n%s\n", a, b, res);
	return 0;
}
